# Railway deploy (auto from main)

1. Railway → Service → Settings → Source
2. Branch: **main**
3. Auto-deploy: ON

Push to main → yangi build.

## Tekshiruv
- `/pari` — kapalak miya
- `/api/mcp` — tools
- `/api/brain/status`

## Obsidian vault
```
GITHUB_TOKEN=...
GITHUB_VAULT_REPO=xojasoipov-sketch/Jarvis-ai
GITHUB_VAULT_PATH=vault
```
